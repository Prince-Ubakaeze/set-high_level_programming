The ``101-lazy_matrix_mul`` module
==================================

Using ``lazy_matrix_mul``
-------------------------

Import the function:

    >>> lazy_matrix_mul = __import__('101-lazy_matrix_mul').lazy_matrix_mul

Multiplying two square matrices:

    >>> print(lazy_matrix_mul([[1, 2], [3, 4]], [[1, 2], [3, 4]]))
    [[ 7 10]
     [15 22]]

Multiplying matrices of different shapes:

    >>> print(lazy_matrix_mul([[1, 2]], [[3, 4], [5, 6]]))
    [[13 16]]

Floats are accepted:

    >>> lazy_matrix_mul([[1.5, 2]], [[2], [1]]).tolist()
    [[5.0]]

Unlike ``100-matrix_mul``, every error below is raised by NumPy, so the
exception type and message depend on the installed NumPy version. The
tests therefore only check that an exception is raised.

An argument that is not a list:

    >>> try:
    ...     lazy_matrix_mul("ALX", [[5, 6], [7, 8]])
    ...     print("no exception")
    ... except Exception:
    ...     print("exception raised")
    exception raised

    >>> try:
    ...     lazy_matrix_mul([[5, 6], [7, 8]], "ALX")
    ...     print("no exception")
    ... except Exception:
    ...     print("exception raised")
    exception raised

An empty argument:

    >>> try:
    ...     lazy_matrix_mul([[]], [[5, 6], [7, 8]])
    ...     print("no exception")
    ... except Exception:
    ...     print("exception raised")
    exception raised

    >>> try:
    ...     lazy_matrix_mul([[5, 6], [7, 8]], [[]])
    ...     print("no exception")
    ... except Exception:
    ...     print("exception raised")
    exception raised

An element that is not a number:

    >>> try:
    ...     lazy_matrix_mul([[5, "6"], [7, 8]], [[5, 6], [7, 8]])
    ...     print("no exception")
    ... except Exception:
    ...     print("exception raised")
    exception raised

    >>> try:
    ...     lazy_matrix_mul([[5, 6], [7, 8]], [[5, "6"], [7, 8]])
    ...     print("no exception")
    ... except Exception:
    ...     print("exception raised")
    exception raised

Rows of different sizes:

    >>> try:
    ...     lazy_matrix_mul([[5, 6, 10], [7, 8]], [[5, 6], [7, 8]])
    ...     print("no exception")
    ... except Exception:
    ...     print("exception raised")
    exception raised

    >>> try:
    ...     lazy_matrix_mul([[5, 6], [7, 8]], [[5, 6, 1], [7, 8]])
    ...     print("no exception")
    ... except Exception:
    ...     print("exception raised")
    exception raised

Matrices that cannot be multiplied:

    >>> try:
    ...     lazy_matrix_mul([[1, 2, 3], [3, 4, 5]], [[1, 2], [3, 4]])
    ...     print("no exception")
    ... except Exception:
    ...     print("exception raised")
    exception raised

Missing one argument:

    >>> lazy_matrix_mul([[1, 2]])
    Traceback (most recent call last):
    TypeError: lazy_matrix_mul() missing 1 required positional argument: 'm_b'

Missing two arguments:

    >>> lazy_matrix_mul()
    Traceback (most recent call last):
    TypeError: lazy_matrix_mul() missing 2 required positional arguments: 'm_a' and 'm_b'